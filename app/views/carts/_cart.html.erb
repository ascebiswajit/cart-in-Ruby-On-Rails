<div id="<%= dom_id cart %>" >

 <div class="col mb-4">
    <div class="card">
     <img src="<%=(url_for(cart.avatar)) %>" class="img-fluid" alt="Product"  width="100" height="50" > 
      <div class="card-body">
      <h5 class="card-title"><%= cart.title %></h5>
        <p class="card-text"><%= cart.description %></p> 
         <p class="card-text"><%= cart.price %></p> 
      </div>
    </div>

  </div>
  </div>


    


